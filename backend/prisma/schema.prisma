// ... existing schema code

model WarehouseNode {
  id          String   @id @default(uuid())
  nodeId      String   @unique
  nodeType    String   @default("Warehouse")
  unitPrice   Decimal  @db.Decimal(18, 2)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("warehouse_nodes")
}